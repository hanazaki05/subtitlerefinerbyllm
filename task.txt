## 9. 后续可扩展方向（非必须）

* 支持 `.srt` 等其他字幕格式
* 增加“diff 报告”：展示修改最多的句子
* 加一个简单 GUI 或 Web 界面
* 支持多线程 / 并发处理多个文件（注意 API 限速）


## 10. 给 Claude Code 的明确任务说明（可以直接贴）

> 请根据 `plan.md` 这个实现方案：
>
> 1. 使用 Python 实现完整工具，按文件结构拆分模块；
> 2. 使用 OpenAI 风格的 API（或我指定的 SDK）实现 `llm_client.py`；
> 3. 完成 `.ass` 解析、chunk 切分、LLM 调用、global_memory 管理、结果写回的全流程；
> 4. 提供一个 CLI `main.py`，支持 `python main.py input.ass output.ass` 的用法；
> 5. 保持代码可读性，适当加注释和少量测试用例，注释和测试用例用**英语**写。
